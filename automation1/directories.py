import os
print(os.getcwd)

# to make directory
os.mkdir("new_dir")
# to change directory
os.chdir("new_dir")

# to remove directory
os.rmdir("new_dir")

# to find the condent of directory
os.listdir("new_dir")